'use strict';

/**
 * discount-promotion router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::discount-promotion.discount-promotion');
