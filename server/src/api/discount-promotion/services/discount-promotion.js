'use strict';

/**
 * discount-promotion service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::discount-promotion.discount-promotion');
